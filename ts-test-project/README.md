A test project for using Editor in TypeScript
==============================================

Run `./node_modules/.bin/webpack` in order to compile `src/ts/test.ts` to `dist/test.js`.

Open the browser on `dist/demo.html`.